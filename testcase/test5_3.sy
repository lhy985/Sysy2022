int arr[3] = {1,2,3};

float area(float l, float w){
    return l * w;
}

float r=1;
int main() {
    float b = r[1];
    float len = 11.2, width = 20.5, height = 5.0;
    float a1 = area(len);
    float a2 = area(len, width, height);
    float a3 = area(len, width);

    //foo(st);
    //mymax(st, st);
    foo = arr[a3];

    return 0;
}

/*
case_3.sy:9 subscripted value is not an array
case_3.sy:11 too few arguments to function ‘area’
case_3.sy:12 too many arguments to function ‘area’
case_3.sy:17 ‘foo’ undeclared
case_3.sy:17 array subscript is not an integer
*/